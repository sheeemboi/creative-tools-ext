export default function Navbar() {
  return (
    <nav className="bg-zinc-800 h-[calc(100vh-6rem)] w-72 fixed overflow-y-scroll scrollbar-none hover:scrollbar-thin scrollbar-track-transparent scrollbar-thumb-zinc-600">
      <ol></ol>
    </nav>
  );
}
